version: 1
early-commands:
        - echo a
        - sleep 1
        - echo a
late-commands:
        - echo a
        - sleep 1
        - echo a
keyboard:
        layout: gb
interactive-sections:
        - keyboard
        - snaps
snaps:
        - name: etcd
          channel: 3.2/stable
packages:
        - dns-server^
        - foo-bar=1.1
snaps:
        - name: etcd
          channel: 3.2/stable
debconf-selections: |
        wtf wtf
